import React from 'react'

export const StatefulComponentInReact = () => {
  return (
    <div>
      <h1 className='text-2xl font-bold' >Stateful Component in React</h1>
      <p>A stateful component in React is a component that can hold and manage its own internal state. This state is used to store dynamic data that can change over time, and when the state changes, the component re-renders to reflect the updated state.</p>
    </div>
  )
}
